services:
  docusaurus:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run start -- --host 0.0.0.0
    container_name: coding-notebook
